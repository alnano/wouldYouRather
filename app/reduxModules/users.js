//action creators on auth / users



const initialState = {
  test: 'test'
}

const users = (state = initialState, action) => {
  switch(action.type) {
    // case // 1
    //   return {

    //   }
      default: 
        return state

    }

}

export default users